Exercise 5: CTE

WITH CustomerOrders AS
(
SELECT
CustomerID,
COUNT(*) AS TotalOrders
FROM Orders
GROUP BY CustomerID
)

SELECT
CustomerID,
TotalOrders
FROM CustomerOrders
WHERE TotalOrders>3;

MERGE → Update existing rows or insert new rows
PIVOT → Convert rows into columns
UNPIVOT → Convert columns back into rows
